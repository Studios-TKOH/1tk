// Aquí guardamos todas las opciones predefinidas de la app para no repetirlas.

enum UserRole { cliente, trabajador }

enum PriorityPlan { estandar, urgente, muyUrgente }

enum ServiceStatus {
  publicado,
  cotizado,
  fondosProtegidos,
  enEjecucion,
  completado,
  liquidacionPendiente,
}
